import 'package:flutter/material.dart';

class singletaskScreen extends StatelessWidget {
  const singletaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Single task sacreeen ');
  }
}
